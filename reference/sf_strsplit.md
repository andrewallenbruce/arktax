# Split string by delimiter

Split string by delimiter

## Usage

``` r
sf_strsplit(x, s, fix = TRUE)
```

## Arguments

- x:

  `<chr>` vector

- s:

  `<chr>` delimiter to split by

- fix:

  `<lgl>` fixed or regex; default is `TRUE`

## Value

`<list>` of split strings
