# Replace by Regex

Replace by Regex

## Usage

``` r
sf_replace(s, p, r, fix = FALSE)
```

## Arguments

- s:

  `<chr>` vector

- p:

  `<chr>` regex pattern

- r:

  `<chr>` replacement

- fix:

  `<lgl>` fixed or regex; default is `FALSE`

## Value

`<chr>` vector with pattern replaced
