# Convert values to `NA`

Convert values to `NA`

## Usage

``` r
na_if(x, y)
```

## Arguments

- x:

  vector to modify

- y:

  values to convert to `NA`. Type must match that of `x`.

## Value

`x` with values in `y` converted to `NA`
