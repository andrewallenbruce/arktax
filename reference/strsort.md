# Parallel Sort

Parallel Sort

## Usage

``` r
strsort(x)
```

## Arguments

- x:

  `<chr>` vector. If other, will default to
  [`base::sort()`](https://rdrr.io/r/base/sort.html)

## Value

`<chr>` vector
