# Remove by Regex

Remove by Regex

## Usage

``` r
sf_remove(s, p, fix = FALSE)
```

## Arguments

- s:

  `<chr>` vector

- p:

  `<chr>` regex pattern

- fix:

  `<lgl>` fixed or regex; default is `FALSE`

## Value

`<chr>` vector with pattern removed
