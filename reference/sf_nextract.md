# Extract Opposite by Regex

Extract Opposite by Regex

## Usage

``` r
sf_nextract(s, p)
```

## Arguments

- s:

  `<chr>` vector

- p:

  `<chr>` regex pattern

## Value

`<chr>` vector with pattern extracted
