# Remove columns and rows with all NAs

Remove columns and rows with all NAs

## Usage

``` r
remove_all_na(x)
```

## Arguments

- x:

  vector or `<data.frame>`

## Value

vector or `<data.frame>` with columns and rows with all NAs removed
